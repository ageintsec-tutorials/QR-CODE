╔════════════════════════════════════════════════════════════════╗
║         STATIC SALESMAN PORTAL - QUICK START GUIDE            ║
║              Ageint Security Solutions                         ║
╚════════════════════════════════════════════════════════════════╝

📋 PREREQUISITES
─────────────────────────────────────────────────────────────────
You need Python 3.7 or higher installed on your computer.

Download from: https://www.python.org/downloads/
⚠️ IMPORTANT: Check "Add Python to PATH" during installation!


🚀 FIRST TIME SETUP (One-time only)
─────────────────────────────────────────────────────────────────
1. Double-click: install.bat
   - This will install all required Python packages
   - Takes about 1-2 minutes
   - You only need to do this ONCE


✨ GENERATING SALESMAN PROFILES
─────────────────────────────────────────────────────────────────
1. Double-click: run.bat
2. Enter the salesman's full name when prompted
3. Files will be generated automatically:
   - HTML profile page → salesmen/
   - QR code image → qrcodes/
   - Admin dashboard updated → index.html


🌐 DEPLOYING TO GITHUB PAGES
─────────────────────────────────────────────────────────────────
Option 1: Automated (Recommended)
   1. Double-click: deploy.bat
   2. Enter a commit message (e.g., "Add John Smith profile")
   3. Script will automatically commit and push to GitHub

Option 2: Manual
   1. Open Command Prompt or PowerShell
   2. Run these commands:
      git add .
      git commit -m "Add new salesman"
      git push origin main


📁 FILE DESCRIPTIONS
─────────────────────────────────────────────────────────────────
install.bat         → One-time setup installer
run.bat             → Generate new salesman profiles
deploy.bat          → Quick deploy to GitHub
generate_salesman.py → Main Python script
requirements.txt    → Python dependencies list
README.md           → Full documentation


🔧 FIRST TIME GIT SETUP
─────────────────────────────────────────────────────────────────
If you haven't set up Git yet:

1. Install Git: https://git-scm.com/downloads

2. Open Command Prompt in this folder and run:
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ageintsec-tutorials/QR-CODE.git
   git branch -M main
   git push -u origin main

3. Enable GitHub Pages:
   - Go to: https://github.com/ageintsec-tutorials/QR-CODE/settings/pages
   - Source: main branch
   - Folder: / (root)
   - Click Save


🌍 ACCESSING YOUR SITE
─────────────────────────────────────────────────────────────────
After deploying to GitHub:

Admin Dashboard:
https://ageintsec-tutorials.github.io/QR-CODE/

Individual Salesman:
https://ageintsec-tutorials.github.io/QR-CODE/salesmen/[name].html

(Replace [name] with the salesman's slug, e.g., "john-smith")


💡 TIPS
─────────────────────────────────────────────────────────────────
• You can run the generator multiple times
• Regenerating overwrites the old profile (prompts for confirmation)
• QR codes automatically point to the GitHub Pages URL
• All generated files are tracked in Git for deployment
• GitHub Pages updates take 1-2 minutes after pushing


❓ TROUBLESHOOTING
─────────────────────────────────────────────────────────────────
Error: "Python is not installed"
→ Install Python and check "Add to PATH" option

Error: "Required packages not installed"
→ Run install.bat again

Error: "Git push failed"
→ Make sure you've set up the remote (see Git Setup above)

QR codes not showing on website
→ Wait 1-2 minutes for GitHub Pages to rebuild
→ Clear browser cache


📞 SUPPORT
─────────────────────────────────────────────────────────────────
For help, check README.md or contact your development team.


═══════════════════════════════════════════════════════════════════
Built with Python, Jinja2, and Materialize CSS
═══════════════════════════════════════════════════════════════════
